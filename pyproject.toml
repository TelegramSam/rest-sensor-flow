[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]

[tool.coverage.run]
source = ["custom_components/json_sensor"]
omit = ["tests/*"]

[tool.coverage.report]
show_missing = true
